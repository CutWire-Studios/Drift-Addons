node_modules/
dist/
.secrets/
.wrangler/
worker/wrangler.local.jsonc
worker-configuration.d.ts
__pycache__/
*.pyc
Archive.zip

# staging/: commit fonts, images, JSON, shaders. Large weights/libs stay out of git;
# a sibling <name>.sha256 is committed instead (see recipes/staging_hashes.py).
staging/.cache/
staging/**/*.onnx
staging/**/*.bin
staging/**/*.so
staging/**/*.so.*
staging/**/*.dylib
staging/**/*.dll
!staging/**/*.sha256

# Upstream gl-transitions checkout, fetched by hand and pinned in
# content/transitions/ATTRIBUTION.md; recipes/import-gl-transitions.py prints the clone command.
vendor/
